seed: 41
data:
  context_steps: 24
  horizons: [6, 12]
  stride: 3
  expected_interval_minutes: 5
  adaptation_fraction: 0.30
  calibration_fraction: 0.15
  max_windows_per_subject: 5000
model:
  input_channels: 4
  hidden_dim: 48
  levels: 3
  kernel_size: 3
  dropout: 0.10
  patient_code_dim: 12
  adapter_rank: 8
training:
  batch_size: 512
  population_epochs: 8
  adapter_pretrain_epochs: 4
  patient_adaptation_epochs: 40
  learning_rate: 0.001
  adaptation_learning_rate: 0.03
  patient_code_l2: 0.01
  interval_alpha: 0.10
  personalization_gate_min_relative_improvement: 0.01
